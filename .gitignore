# Files and directories created by pub
.dart_tool/
.packages

# Library packages do not typically check in pubspec.lock
pubspec.lock

# Conventional directory for build output.
build/

# Directory created by dartdoc
doc/api/

# Don't need to include code coverage in the package
coverage/

**/*.so
**/*.dylib
**/*.dll
**/*.obj
**/*.exe
**/*.aot

**/*.pdb
**/*.ilk

# Remove this if it gets generated by running an example
**/golden.txt

# Test scratchpad for development
example/scratch.dart